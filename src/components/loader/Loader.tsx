import React from "react";
import "./Loader.scss"; // Include this file to style the loader

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
    <p>Loading...</p>
  </div>
);

export default Loader;
